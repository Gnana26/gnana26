package com.example.projects; // Ensure this matches your package declaration

import io.flutter.embedding.android.FlutterActivity; // Import FlutterActivity

public class MainActivity extends FlutterActivity {
    // No need to override any methods unless you need custom behavior.
}
